# 仕様書：アルゴリズムの可視化システム

## 1. 概要
ユーザーがアルゴリズム（ソートや探索など）の動作を視覚的に理解できるWebアプリケーション。

## 2. デザイン
（StitchやFigmaなどのツールで作成したモックアップのリンク、または簡単な画面構成を記載します）
- **画面構成例**:
  - ヘッダー: タイトル
  - コントロールパネル: アルゴリズム選択（例: バブルソート、クイックソート）、データ数指定、実行速度調整、開始/停止/リセットボタン
  - 可視化エリア: 配列データを示す棒グラフやブロックのアニメーション領域

## 3. 機能要件 (Functional Requirements)
- [x] ユーザーは複数のアルゴリズム（バブルソート、クイックソート）から1つを選択できる
- [x] ユーザーはデータの要素数（例: 10〜10000）を指定してランダムなデータを生成できる
- [x] ユーザーは「開始」ボタンでアルゴリズムの可視化アニメーションを再生できる
- [x] ユーザーはアニメーションの再生速度を変更できる
- [x] ユーザーは「リセット」ボタンで初期状態に戻すことができる
- [x] **サウンド効果**: ソート中、値の入れ替えが発生したタイミングで、対象となる値の大きさに応じた音程の電子音を鳴らし、心地よいフィードバックを提供する
- [x] **グラデーションカラーバー**: 棒の色を値に応じたグラデーション（虹色）にする。ソートが進むにつれて美しい色の並びが完成していく
- [x] **ソート完了時のスウィープアニメーション**: ソート完了後、左端から右端へ棒が順番に光る「完了演出」を入れる
- [x] **比較・交換中のハイライト**: 現在比較中の要素や交換中の要素を色で区別する（比較中＝黄色、交換中＝赤）

## 4. 非機能要件 (Non-functional Requirements)
- [x] レスポンシブ対応（PCおよびスマートフォンからの閲覧をサポート）
- [x] アニメーションはスムーズに動作すること（HTML、CSS、JavaScriptで作成）
- [x] ソート中の配列の要素数の上限は10000とする
- [x] ソート中の配列の要素数の最小値は1000とする


## 5. テストケース (Test Cases)
- [x] **TC1**: ページロード時にデフォルトの配列が可視化エリアに表示されていること。
- [x] **TC2**: アルゴリズムを選択し、開始ボタンを押すと、配列が正しくソート/処理され、その過程がアニメーション表示されること。
- [x] **TC3**: 処理中に「リセット」を押すと、アニメーションが停止し、新しい配列が生成されること。
- [x] **TC4**: スライダー等で速度を変更した場合、アニメーションの速度が即座に反映されること。
- [x] **TC5**: 棒グラフの色が値に応じた虹色グラデーションで表示されていること。ソート完了後に色が正しい順序で並ぶこと。
- [x] **TC6**: ソート完了後、左端から右端へ棒が順番に光るスウィープアニメーションが再生されること。
- [x] **TC7**: ソート中、比較中の要素が黄色、交換中の要素が赤色でハイライトされること。
